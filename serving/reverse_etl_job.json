{
  "_comment": "Stage 3 serving — serverless reverse-ETL job. Deploy: upload serving/reverse_etl.py to the workspace, then databricks jobs create --json @serving/reverse_etl_job.json (update the notebook/file path + existing schedule to taste). Runs on serverless with psycopg as a library; refreshes Lakebase daily.",
  "name": "dev-churn-reverse-etl-serving",
  "tasks": [
    {
      "task_key": "reverse_etl",
      "spark_python_task": {
        "python_file": "/Workspace/Users/cathy.snell@databricks.com/usage_events_ingest/serving/reverse_etl.py"
      },
      "environment_key": "serving_env"
    }
  ],
  "environments": [
    {
      "environment_key": "serving_env",
      "spec": {
        "client": "3",
        "dependencies": ["psycopg[binary]"]
      }
    }
  ],
  "schedule": {
    "quartz_cron_expression": "0 0 6 * * ?",
    "timezone_id": "UTC",
    "pause_status": "PAUSED"
  },
  "queue": {"enabled": true}
}
