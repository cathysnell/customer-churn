# Runtime dependencies for the synthetic dataset generator.
# Pure Python — no Databricks runtime needed to generate the data locally.
numpy>=1.24
pandas>=2.0
pyarrow>=12.0

# Dev / test (also available as: pip install -e ".[dev]")
pytest>=7.4
ruff>=0.4
